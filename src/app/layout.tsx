import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "World Cup 2026 — Knockout Bracket",
  description: "Interactive World Cup 2026 knockout bracket. Round of 32 → Round of 16 → Quarterfinals → Semifinals → Final. Mirror layout with both halves meeting at the center final.",
  keywords: ["World Cup 2026", "knockout bracket", "football", "soccer", "tournament", "Round of 32"],
  authors: [{ name: "Z.ai" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "World Cup 2026 Knockout Bracket",
    description: "Interactive mirror-layout bracket: 32 teams, 31 matches, final at center.",
    siteName: "World Cup 2026",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
